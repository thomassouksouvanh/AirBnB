{% block navbar %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-info">
        <h1>| AirBnB |</h1>
        <h3>Administration</h3>
        <a class="navbar-brand ml-3" href="/" ><i class="fas fa-chart-line"></i>Dashboard</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path('admin_index_annonce') }}">
                        <i class="fas fa-hotel"></i>Annonces</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path('admin_reservation_index') }}"><i class="fas fa-folder"></i>Reservations</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path('admin_comments') }}"><i class="fas fa-comments"></i>Commentaires</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="#"><i class="fas fa-users"></i>Utilisateurs</a>
                </li>
            </ul>
            <ul>
                <div class="navbar-nav ml-auto">
                    <a href="{{ path('home_index') }}" target="_blank" class="nav-link"><i class="fas fa-eye">Voir le site</i></a>
                </div>
            </ul>


            {% if app.user %}
                <img src="{{ app.user.picture }}" class="avatar-mini" alt="photo administrateur">
                <a class="text-white" href="{{ path('account_logout') }}">Déconnexion</a>
            {% endif %}
        </div>
    </nav>
    {% for label, messages in app.flashes %}
        <div class="container">
            <div class="alert alert-{{ label }}">
                {% for message in messages %}
                    <p>{{ message | raw }}</p>
                {% endfor %}
            </div>
        </div>
    {% endfor %}
{% endblock %}

